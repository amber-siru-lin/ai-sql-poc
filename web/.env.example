# Copy to web/.env.local (gitignored). Function URL from sandbox output or deploy/clients README.

VITE_API_URL=http://localhost:8000
VITE_COPILOT_RUNTIME_URL=http://localhost:8000/copilotkit

# Set to true after sandbox deploy to require Cognito sign-in (needs amplify_outputs.json auth block)
VITE_REQUIRE_AUTH=false

# Optional — same as local api/ when developing hybrid
# VITE_EDITOR_AGENT_URL=http://localhost:8000/semantic-agent
