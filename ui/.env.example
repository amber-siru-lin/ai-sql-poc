# CopilotKit UI → FastAPI agent URL (Vite requires full URL, not a relative path)
VITE_API_URL=http://localhost:8000
# CopilotKit runtime prefix (must expose /info — see api/main.py)
VITE_COPILOT_RUNTIME_URL=http://localhost:8000/copilotkit
