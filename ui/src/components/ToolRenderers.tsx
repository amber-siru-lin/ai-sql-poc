/**
 * Custom tool renderers for CopilotKit chat.
 * MVP: stubs only — Phase 3.3 will replace with rich SQL / table cards.
 */
export function registerToolRenderers() {
  // Phase 3.3 — wire useRenderToolCall for:
  // - execute_snowflake_sql → SqlResultCard
  // - get_schema_summary → SchemaSnippetCard
  return null;
}
